[project]
name = "satoshi-gemakachi"
version = "0.1.0"
description = "Safe BTC/satoshi conversions"
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
